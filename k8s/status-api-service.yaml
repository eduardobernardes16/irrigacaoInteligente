apiVersion: v1
kind: Service

metadata:
  name: status-api
  namespace: irrigacao

spec:
  type: NodePort

  selector:
    app: status-api

  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30000